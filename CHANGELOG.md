# Changelog

## v0.1.0

- Added FastAPI HTTP service with `/ping`, `/healthz`, and `/version`
- Containerized the application with Docker
- Added Helm chart for Kubernetes deployment
- Added dev and prod Helm values
- Added GitHub Actions CI pipeline
- Added lint, test, Docker build, Trivy scan, gitleaks scan, and GHCR push
