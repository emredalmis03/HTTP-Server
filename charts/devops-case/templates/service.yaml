apiVersion: v1
kind: Service
metadata:
  name: {{ include "devops-case.fullname" . }}
  labels:
    {{- include "devops-case.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "devops-case.selectorLabels" . | nindent 4 }}
